/**
 * 本文档临时用于定义debugger返回的各种结果的格式
 */

/**
 * 开启debug后的返回结果
 */
var debug = {
    //开启的debug会话的编号
    "debugId":1
};

/**
 * 添加断点
 */
var breakPoint = {
    //添加的断点个数
    "breakPointNum":2
};

//其他类型的结果请参见parser.js内的配置部分